<div class="container">
  <ul>
    <div class="row">
      <div class="col-7 offset-1">
        <h2>注文履歴 <i class="fas fa-dog"></i></h2>
      </div>
    </div>
    <div class="row">
      <div class="col-8 offset-2">
         <ul class="list-group">
            <% @orders.each do |order| %>
              <%= link_to(order_path(order), class: "list-group-item list-group-item-action") do %>
                <%= order.created_at %>
              <% end %>
            <% end %>
          </ul>
        </div>
      </div>
    </ul>
  </div>